- Right `if` operator arguments order
- pattern-matching
- https://github.com/cosmologicon/pywat
  - reversed(a) == reversed(a) ==> False
    - компрехеншн = список
- имена внутри comprehension
- inline-c, а в него asm (проверить)
- пример реальной программы — вебчик
  - посмеяться: явные опечатки, скриншоты файрфокса
- вставить главу про портирование из 5 в 3 — functools, itertools
- простота параллелизма
  - загрузить из интернета URL и обработать
- нельзя присвоить в переменную из вышестоящего скоупа
  - исправлено с помощью non-local
- передавать блоки в функции вместо context managers
- Каналы как в Го
- статические шаблоны для веба
  - слишком мало или слишком много эскейпинга в шаблонах
