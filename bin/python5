#!/bin/bash
set -eu
set -o pipefail

if [[ ! -z "${PYTHON5_LOCALTEST:-}" ]]; then
    package_db_opt="-package-db=dist/package.conf.inplace"
fi

exts="-XExtendedDefaultRules -XNoImplicitPrelude"
opts="$exts ${package_db_opt:-}"

runhaskell $opts "$@"
